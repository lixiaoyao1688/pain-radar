{
  "name": "pain-radar",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev:web": "turbo run dev -F web",
    "dev:server": "turbo run dev -F server",
    "db:push": "turbo run db:push -F @pain-radar/db",
    "db:studio": "turbo run db:studio -F @pain-radar/db",
    "db:generate": "turbo run db:generate -F @pain-radar/db",
    "db:migrate": "turbo run db:migrate -F @pain-radar/db",
    "check": "ultracite check",
    "fix": "ultracite fix"
  },
  "dependencies": {
    "@pain-radar/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@pain-radar/config": "workspace:*",
    "@types/node": "catalog:",
    "turbo": "^2.10.2",
    "typescript": "catalog:",
    "ultracite": "7.8.4"
  },
  "packageManager": "pnpm@10.12.1"
}
