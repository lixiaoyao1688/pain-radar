# 开发计划索引

## 本次 PRD（2026-07-04）切分为 1 个 feature

| 序号 | feature           | 说明                               | 依赖 | 状态   |
| ---- | ----------------- | ---------------------------------- | ---- | ------ |
| 1    | pain-point-module | 痛点录入存库 + 痛点浏览与搜索      | -    | 待开发 |

**推荐执行顺序**：1

## ID 编号约定

- 功能需求 / 任务 / 验收标准 ID **在单个 feature 内编号**，跨 feature 用 `{序号}.` 前缀区分。
- 例：`1.T-001` = 序号 1 这个 feature 的 T-001。
- **跨 feature 依赖**写全限定 ID（本次仅 1 个 feature，暂无跨 feature 依赖）。
